rules_version = '2';

// All reads and writes go through the Next.js server using the Admin SDK, which
// bypasses these rules. Block direct client access to every collection.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
